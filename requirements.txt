# ============================================================
# VendorScout Pro - Requirements
# ============================================================
# Python dependencies for the backend application.
# Pinned to major.minor for reproducible builds.
# ============================================================

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.0

# Template rendering
jinja2==3.1.4

# Async HTTP client (Azure OpenAI / OpenAI-compatible LLM, SerperDev API calls)
httpx==0.27.0

# Self-hosted agentic browser (Microsoft Playwright) — after install run:
#   playwright install chromium
playwright==1.47.0

# Async SQLite database
aiosqlite==0.20.0

# LangGraph for multi-agent orchestration
langgraph==0.2.0

# Pydantic for data validation
pydantic==2.9.0
pydantic-settings==2.5.0

# Environment variable loading
python-dotenv==1.0.1
